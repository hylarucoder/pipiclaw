{
  "name": "@pipiclaw/renderer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev --config vite.config.ts",
    "preview": "vite preview --config vite.config.ts"
  },
  "dependencies": {
    "@pipiclaw/agent-core": "workspace:*",
    "@pipiclaw/shared": "workspace:*"
  }
}
